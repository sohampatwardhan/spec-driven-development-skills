interface:
  display_name: "Spec Audit"
  short_description: "Audit spec artifacts before execution"
  default_prompt: "Use $spec-audit to scrutinize the requirements, design, and tasks before execution."
